description: Not found
content:
  application/json:
    schema:
      allOf:
        - $ref: ../schemas/Envelope.yaml
        - type: object
          properties:
            status:  { example: 404 }
            message: { example: "Not found" }
            data:    { type: object, example: {} }
