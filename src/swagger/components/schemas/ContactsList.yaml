type: object
properties:
  page:     { type: integer, example: 1 }
  perPage:  { type: integer, example: 10 }
  total:    { type: integer, example: 42 }
  items:
    type: array
    items: { $ref: ./Contact.yaml }
required: [ page, perPage, total, items ]
