tags: [Auth]
summary: Refresh session (via refresh cookie)
operationId: refresh
responses:
  "200":
    description: OK
    content:
      application/json:
        schema: { $ref: ../../components/schemas/AuthTokens.yaml }
