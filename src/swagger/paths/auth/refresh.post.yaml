tags: [Auth]
summary: Refresh session (via refresh cookie)
operationId: refresh
responses:
  "200":
    description: OK
    content:
      application/json:
        schema: { $ref: ../../components/schemas/AuthTokens.yaml }
  "401":
    $ref: ../../components/responses/401.yaml
  "500":
    $ref: ../../components/responses/500.yaml
